spring:
  application:
    name: sns

# datasource:
#     url: jdbc:h2:mem:testdb
#     driver-class-name: org.h2.Driver
#     username: sa
#     password: 
    
# jpa:
#   hibernate:
#     ddl-auto: update
#   show-sql: true
jwt:
  secret: study-jwt-secret-key-must-be-at-least-32-bytes-long
  access-token-expiration-millis: 3600000